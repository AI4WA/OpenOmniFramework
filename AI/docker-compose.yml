version: '3.8'

services:
  worker_cpu:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: worker_cpu
    command: /app/entrypoint.sh
    volumes:
      - .:/app
    environment:
      - TOKEN=38e1e473481edb8230d1b2c3d3c48bbdcfde617e
      - TASK_TYPE=cpu
      - API_DOMAIN=http://localhost:8000
    network_mode: host
