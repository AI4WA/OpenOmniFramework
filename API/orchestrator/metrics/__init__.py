"""
This will pull all the data from the task records
And then summary the data to performance metrics

The metrics will be:

- For each cluster, it will be divided into components:
    - each component total time
    - combined time.
    - And if we do not queue it with delay, end to end time. (This should be same as above)

"""
